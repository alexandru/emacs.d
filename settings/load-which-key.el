;;; load-which-key.el --- -*- lexical-binding: t; -*-

;;; Commentary:
;;
;; For displaying available key bindings
;; `https://github.com/justbur/emacs-which-key'
;;

;;; Code:

(use-package which-key
  :ensure t
  :config
  (which-key-mode))

(provide 'load-which-key)
;;; load-which-key.el ends here
